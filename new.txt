// pages/DoctorList.jsx
import { Stethoscope, Star, MapPin, Search, Filter, ShieldCheck, ArrowRight, Clock, Award, ChevronDown, SlidersHorizontal, X } from "lucide-react";
import { useState } from "react";
import { Link } from "react-router-dom";

const DoctorList = () => {
  const [searchTerm, setSearchTerm] = useState("");
  const [selectedSpecialty, setSelectedSpecialty] = useState("all");
  const [showFilters, setShowFilters] = useState(false);
  
  const doctors = [
    {
      id: 1,
      name: "Dr. John Smith",
      specialty: "Cardiologist",
      rating: "4.9",
      reviews: "120",
      location: "Main Medical Center",
      experience: "12 years",
      education: "Harvard Medical School",
      availability: "Available Today",
      bio: "Specialist in non-invasive cardiology and cardiovascular health. Focused on preventive care and heart disease management.",
      image: "JS"
    },
    {
      id: 2,
      name: "Dr. Sarah Jenkins",
      specialty: "Neurologist",
      rating: "4.8",
      reviews: "85",
      location: "West Wing Clinic",
      experience: "8 years",
      education: "Johns Hopkins University",
      availability: "Tomorrow",
      bio: "Expert in neuro-diagnostics and treatment of chronic migraines. Pioneer in minimally invasive neurological procedures.",
      image: "SJ"
    },
    {
      id: 3,
      name: "Dr. Michael Chen",
      specialty: "Pediatrician",
      rating: "5.0",
      reviews: "210",
      location: "Children's Health Plaza",
      experience: "15 years",
      education: "Stanford University",
      availability: "Available Today",
      bio: "Dedicated to comprehensive pediatric care and childhood development. Specializes in adolescent medicine.",
      image: "MC"
    },
    {
      id: 4,
      name: "Dr. Emily Rodriguez",
      specialty: "Dermatologist",
      rating: "4.9",
      reviews: "156",
      location: "Skin Health Institute",
      experience: "10 years",
      education: "Yale School of Medicine",
      availability: "Wednesday",
      bio: "Board-certified dermatologist specializing in medical and cosmetic dermatology, skin cancer screening.",
      image: "ER"
    },
    {
      id: 5,
      name: "Dr. James Wilson",
      specialty: "Orthopedist",
      rating: "4.7",
      reviews: "94",
      location: "Sports Medicine Center",
      experience: "14 years",
      education: "Columbia University",
      availability: "Thursday",
      bio: "Sports medicine specialist with expertise in joint preservation and arthroscopic surgery.",
      image: "JW"
    },
    {
      id: 6,
      name: "Dr. Lisa Thompson",
      specialty: "Psychiatrist",
      rating: "4.9",
      reviews: "178",
      location: "Mental Wellness Center",
      experience: "11 years",
      education: "UCSF Medical Center",
      availability: "Available Today",
      bio: "Compassionate psychiatrist providing evidence-based treatment for anxiety, depression, and mood disorders.",
      image: "LT"
    }
  ];

  const specialties = ["All Specialties", "Cardiologist", "Neurologist", "Pediatrician", "Dermatologist", "Orthopedist", "Psychiatrist"];

  const filteredDoctors = doctors.filter(doctor => {
    const matchesSearch = doctor.name.toLowerCase().includes(searchTerm.toLowerCase()) || 
                         doctor.specialty.toLowerCase().includes(searchTerm.toLowerCase()) ||
                         doctor.bio.toLowerCase().includes(searchTerm.toLowerCase());
    const matchesSpecialty = selectedSpecialty === "all" || 
                            doctor.specialty.toLowerCase().includes(selectedSpecialty.toLowerCase());
    return matchesSearch && matchesSpecialty;
  });

  return (
    <div className="max-w-7xl mx-auto px-4 py-8 space-y-8 animate-in fade-in duration-500">
      {/* Enhanced Header with Background */}
      <div className="relative bg-gradient-to-br from-indigo-600 to-purple-600 rounded-[2.5rem] p-8 overflow-hidden shadow-2xl">
        <div className="absolute top-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl"></div>
        <div className="absolute bottom-0 left-0 w-64 h-64 bg-purple-500/20 rounded-full blur-3xl"></div>
        
        <div className="relative z-10 text-white">
          <h1 className="text-4xl md:text-5xl font-black tracking-tight">Find Your Doctor</h1>
          <p className="text-indigo-100 text-lg mt-3 max-w-2xl">
            Browse our network of 50+ board-certified specialists. Book appointments instantly with availability today.
          </p>
          
          {/* Search Bar in Header */}
          <div className="mt-8 flex flex-col md:flex-row gap-4 max-w-3xl">
            <div className="flex-1 relative group">
              <Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 group-focus-within:text-indigo-600 transition-colors" size={20} />
              <input 
                type="text" 
                placeholder="Search by doctor name, specialty, or condition..."
                value={searchTerm}
                onChange={(e) => setSearchTerm(e.target.value)}
                className="w-full pl-12 pr-4 py-4 bg-white text-slate-800 placeholder:text-slate-400 rounded-2xl text-sm focus:outline-none focus:ring-4 focus:ring-indigo-500/30 shadow-lg"
              />
            </div>
            <button 
              onClick={() => setShowFilters(!showFilters)}
              className="px-6 py-4 bg-white/10 backdrop-blur-sm hover:bg-white/20 rounded-2xl font-semibold text-sm flex items-center justify-center gap-2 transition-all border border-white/20"
            >
              <SlidersHorizontal size={18} />
              Filters
              {selectedSpecialty !== "all" && (
                <span className="w-5 h-5 bg-white rounded-full text-indigo-600 text-xs flex items-center justify-center font-bold">
                  {1}
                </span>
              )}
            </button>
          </div>
        </div>
      </div>

      {/* Filters Panel */}
      {showFilters && (
        <div className="bg-white rounded-2xl p-6 shadow-xl border border-slate-100 animate-in slide-in-from-top-2 duration-300">
          <div className="flex items-center justify-between mb-6">
            <h3 className="font-bold text-slate-800">Filter Doctors</h3>
            <button 
              onClick={() => {
                setSelectedSpecialty("all");
                setShowFilters(false);
              }}
              className="text-xs text-slate-500 hover:text-slate-700 flex items-center gap-1"
            >
              <X size={14} />
              Clear filters
            </button>
          </div>
          <div className="flex flex-wrap gap-3">
            {specialties.map((specialty) => (
              <button
                key={specialty}
                onClick={() => setSelectedSpecialty(specialty === "All Specialties" ? "all" : specialty.toLowerCase())}
                className={`px-5 py-2.5 rounded-xl text-sm font-semibold transition-all ${
                  (selectedSpecialty === (specialty === "All Specialties" ? "all" : specialty.toLowerCase()))
                    ? "bg-indigo-600 text-white shadow-lg shadow-indigo-200"
                    : "bg-slate-50 text-slate-600 hover:bg-slate-100"
                }`}
              >
                {specialty}
              </button>
            ))}
          </div>
        </div>
      )}

      {/* Results Header */}
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-4">
          <h2 className="text-2xl font-bold text-slate-800">
            {filteredDoctors.length} Doctors Available
          </h2>
          <span className="px-3 py-1.5 bg-indigo-50 text-indigo-700 rounded-lg text-xs font-bold">
            {filteredDoctors.filter(d => d.availability === "Available Today").length} available today
          </span>
        </div>
        <select className="px-4 py-2 bg-slate-50 border border-slate-200 rounded-xl text-sm focus:outline-none focus:ring-4 focus:ring-indigo-500/10">
          <option>Sort by: Most Relevant</option>
          <option>Sort by: Highest Rated</option>
          <option>Sort by: Most Experienced</option>
          <option>Sort by: Soonest Available</option>
        </select>
      </div>

      {/* Doctor Grid */}
      {filteredDoctors.length > 0 ? (
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {filteredDoctors.map((doc, index) => (
            <div 
              key={doc.id} 
              className="group relative bg-white rounded-2xl p-6 shadow-lg shadow-slate-200/50 border border-slate-100 hover:border-indigo-200 hover:shadow-xl transition-all duration-300 animate-in slide-in-from-bottom-4"
              style={{ animationDelay: `${index * 100}ms` }}
            >
              {/* Availability Badge */}
              <div className="absolute top-6 right-6">
                <span className={`px-3 py-1.5 rounded-lg text-xs font-bold ${
                  doc.availability === "Available Today" 
                    ? "bg-emerald-50 text-emerald-700 border border-emerald-200" 
                    : "bg-amber-50 text-amber-700 border border-amber-200"
                }`}>
                  {doc.availability}
                </span>
              </div>

              {/* Doctor Avatar & Rating */}
              <div className="flex items-start gap-4 mb-5">
                <div className="relative">
                  <div className="w-20 h-20 rounded-2xl bg-gradient-to-br from-indigo-50 to-indigo-100 flex items-center justify-center text-indigo-600 text-2xl font-bold">
                    {doc.image}
                  </div>
                  <div className="absolute -bottom-1 -right-1 w-6 h-6 bg-white rounded-lg shadow-md flex items-center justify-center">
                    <ShieldCheck size={14} className="text-blue-500" />
                  </div>
                </div>
                <div className="flex-1">
                  <div className="flex items-center gap-2 mb-1">
                    <h2 className="text-lg font-bold text-slate-800">{doc.name}</h2>
                  </div>
                  <p className="text-sm font-semibold text-indigo-600 uppercase tracking-wide">{doc.specialty}</p>
                  <div className="flex items-center gap-1 mt-2">
                    <Star size={14} className="text-amber-500 fill-amber-500" />
                    <span className="text-sm font-bold text-slate-700">{doc.rating}</span>
                    <span className="text-xs text-slate-400">({doc.reviews} reviews)</span>
                  </div>
                </div>
              </div>

              {/* Doctor Bio */}
              <p className="text-sm text-slate-600 leading-relaxed line-clamp-2 mb-4">
                {doc.bio}
              </p>

              {/* Qualifications & Location */}
              <div className="space-y-3 mb-5">
                <div className="flex items-center gap-2 text-xs">
                  <Award size={14} className="text-indigo-500" />
                  <span className="text-slate-600">{doc.education}</span>
                </div>
                <div className="flex items-center gap-2 text-xs">
                  <MapPin size={14} className="text-slate-400" />
                  <span className="text-slate-600">{doc.location}</span>
                </div>
                <div className="flex items-center gap-2 text-xs">
                  <Clock size={14} className="text-slate-400" />
                  <span className="text-slate-600">{doc.experience} experience</span>
                </div>
              </div>

              {/* Action Buttons */}
              <div className="flex gap-3">
                <Link 
                  to={`/doctor/${doc.id}`}
                  className="flex-1 py-3.5 bg-slate-900 hover:bg-slate-800 text-white rounded-xl text-sm font-bold flex items-center justify-center gap-2 transition-all group/btn"
                >
                  View Profile
                  <ArrowRight size={16} className="group-hover/btn:translate-x-1 transition-transform" />
                </Link>
                <Link
                  to="/patient/book-appointment"
                  className="px-5 py-3.5 bg-indigo-50 hover:bg-indigo-100 text-indigo-700 rounded-xl text-sm font-bold transition-all"
                >
                  Book
                </Link>
              </div>
            </div>
          ))}
        </div>
      ) : (
        <div className="text-center py-16 bg-white rounded-2xl border border-slate-100">
          <div className="inline-flex p-4 bg-slate-50 rounded-2xl mb-4">
            <Search size={32} className="text-slate-400" />
          </div>
          <h3 className="text-xl font-bold text-slate-800 mb-2">No doctors found</h3>
          <p className="text-slate-500 mb-6">Try adjusting your search or filter criteria</p>
          <button 
            onClick={() => {
              setSearchTerm("");
              setSelectedSpecialty("all");
            }}
            className="px-6 py-3 bg-indigo-600 text-white rounded-xl font-semibold text-sm hover:bg-indigo-700 transition-all"
          >
            Clear all filters
          </button>
        </div>
      )}
    </div>
  );
};

export default DoctorList;


// pages/BookAppointment.jsx
import { useState } from "react";
import { Calendar, Clock, Stethoscope, FileText, Send, AlertCircle, CheckCircle, ChevronRight, User, MapPin, Phone, Mail, Star, Award, ChevronLeft } from "lucide-react";
import { Link } from "react-router-dom";

const BookAppointment = () => {
  const [form, setForm] = useState({
    doctor: "",
    date: "",
    time: "",
    reason: "",
    isNewPatient: "yes"
  });
  const [step, setStep] = useState(1);
  const [showConfirmation, setShowConfirmation] = useState(false);

  const handleChange = (e) => {
    setForm({ ...form, [e.target.name]: e.target.value });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    setShowConfirmation(true);
    setTimeout(() => {
      setShowConfirmation(false);
      // Reset form
      setForm({
        doctor: "",
        date: "",
        time: "",
        reason: "",
        isNewPatient: "yes"
      });
      setStep(1);
    }, 3000);
  };

  const nextStep = () => {
    if (step === 1 && !form.doctor) {
      alert("Please select a doctor");
      return;
    }
    if (step === 2 && (!form.date || !form.time)) {
      alert("Please select both date and time");
      return;
    }
    setStep(step + 1);
  };

  const prevStep = () => {
    setStep(step - 1);
  };

  const doctors = [
    { 
      id: 1, 
      name: "Dr. John Smith", 
      specialty: "Cardiology", 
      availability: ["Mon", "Wed", "Fri"], 
      image: "JS", 
      rating: 4.9,
      reviews: 120,
      location: "Main Medical Center",
      nextAvailable: "Today"
    },
    { 
      id: 2, 
      name: "Dr. Sarah Jenkins", 
      specialty: "Neurology", 
      availability: ["Tue", "Thu"], 
      image: "SJ", 
      rating: 4.8,
      reviews: 85,
      location: "West Wing Clinic",
      nextAvailable: "Tomorrow"
    },
    { 
      id: 3, 
      name: "Dr. Michael Chen", 
      specialty: "Pediatrics", 
      availability: ["Mon", "Tue", "Thu", "Fri"], 
      image: "MC", 
      rating: 5.0,
      reviews: 210,
      location: "Children's Health Plaza",
      nextAvailable: "Today"
    },
  ];

  // Generate available time slots
  const timeSlots = [
    "09:00 AM", "09:30 AM", "10:00 AM", "10:30 AM", "11:00 AM", "11:30 AM",
    "01:00 PM", "01:30 PM", "02:00 PM", "02:30 PM", "03:00 PM", "03:30 PM", "04:00 PM"
  ];

  if (showConfirmation) {
    return (
      <div className="max-w-4xl mx-auto px-4 py-12 animate-in fade-in duration-500">
        <div className="bg-white rounded-[2.5rem] p-12 shadow-2xl border border-slate-100 text-center">
          <div className="inline-flex p-5 bg-gradient-to-br from-emerald-50 to-green-50 rounded-full mb-8 border-8 border-emerald-50/50">
            <div className="p-2 bg-emerald-100 rounded-full">
              <CheckCircle size={56} className="text-emerald-600" />
            </div>
          </div>
          <h2 className="text-4xl font-black text-slate-800 mb-4">Request Sent Successfully!</h2>
          <p className="text-lg text-slate-600 mb-10 max-w-lg mx-auto">
            Your appointment request has been submitted. The clinic will confirm your booking within 2 hours.
          </p>
          
          <div className="bg-gradient-to-br from-slate-50 to-white rounded-2xl p-8 mb-10 max-w-lg mx-auto text-left border border-slate-200 shadow-xl">
            <div className="flex items-center gap-5 mb-6 pb-6 border-b border-slate-200">
              <div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-indigo-500 to-indigo-600 flex items-center justify-center text-white text-2xl font-bold shadow-lg shadow-indigo-200">
                {doctors.find(d => d.name === form.doctor)?.image || 'DR'}
              </div>
              <div>
                <p className="text-xl font-bold text-slate-800">{form.doctor || "Selected Doctor"}</p>
                <p className="text-sm font-medium text-indigo-600 mt-1">
                  {doctors.find(d => d.name === form.doctor)?.specialty || "Specialist"}
                </p>
              </div>
            </div>
            
            <div className="grid grid-cols-2 gap-4 mb-4">
              <div className="bg-slate-50 p-4 rounded-xl">
                <p className="text-xs text-slate-500 mb-1">Date</p>
                <p className="text-sm font-bold text-slate-800">{form.date || "Feb 12, 2024"}</p>
              </div>
              <div className="bg-slate-50 p-4 rounded-xl">
                <p className="text-xs text-slate-500 mb-1">Time</p>
                <p className="text-sm font-bold text-slate-800">{form.time || "10:30 AM"}</p>
              </div>
            </div>
            
            <div className="flex items-center gap-3 text-xs text-slate-500 mt-2 p-4 bg-amber-50 rounded-xl">
              <Clock size={16} className="text-amber-600 flex-shrink-0" />
              <p className="font-medium">You can track your appointment status in the dashboard. A confirmation email has been sent.</p>
            </div>
          </div>
          
          <div className="flex gap-4 justify-center">
            <Link 
              to="/patient" 
              className="px-8 py-4 bg-slate-900 text-white rounded-xl font-bold text-sm hover:bg-slate-800 transition-all shadow-xl shadow-slate-200 flex items-center gap-2"
            >
              <ChevronLeft size={16} />
              Go to Dashboard
            </Link>
            <button 
              onClick={() => setShowConfirmation(false)}
              className="px-8 py-4 bg-indigo-50 text-indigo-700 rounded-xl font-bold text-sm hover:bg-indigo-100 transition-all border border-indigo-200"
            >
              Book Another
            </button>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="max-w-7xl mx-auto px-4 py-8 space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-500">
      {/* Header with Steps */}
      <div className="bg-gradient-to-br from-indigo-600 via-indigo-500 to-purple-600 rounded-[2.5rem] p-8 text-white shadow-2xl overflow-hidden relative">
        <div className="absolute top-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl"></div>
        <div className="absolute bottom-0 left-0 w-64 h-64 bg-purple-500/20 rounded-full blur-3xl"></div>
        
        <div className="relative z-10">
          <h1 className="text-4xl md:text-5xl font-black tracking-tight mb-4">Book an Appointment</h1>
          <p className="text-indigo-100 text-lg max-w-2xl">
            Schedule your visit with our specialists. Choose your preferred doctor, date, and time.
          </p>
          
          {/* Progress Steps */}
          <div className="flex items-center gap-4 mt-10">
            {[1, 2, 3].map((s) => (
              <div key={s} className="flex items-center">
                <div className={`flex items-center justify-center w-12 h-12 rounded-2xl text-lg font-black transition-all duration-300
                  ${step >= s 
                    ? 'bg-white text-indigo-600 shadow-lg' 
                    : 'bg-white/20 text-white/80 backdrop-blur-sm'
                  }`}
                >
                  {step > s ? <CheckCircle size={20} /> : s}
                </div>
                {s < 3 && (
                  <div className={`w-20 h-1 mx-2 rounded-full transition-all duration-300
                    ${step > s ? 'bg-white' : 'bg-white/20'}`}
                  />
                )}
              </div>
            ))}
          </div>
          <div className="flex gap-20 mt-3 text-sm font-medium">
            <span className={step >= 1 ? 'text-white' : 'text-white/60'}>Select Doctor</span>
            <span className={step >= 2 ? 'text-white' : 'text-white/60'}>Choose Time</span>
            <span className={step >= 3 ? 'text-white' : 'text-white/60'}>Confirm Details</span>
          </div>
        </div>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
        {/* Left: The Form */}
        <form onSubmit={handleSubmit} className="lg:col-span-2 space-y-6">
          <div className="bg-white rounded-[2rem] p-8 shadow-xl shadow-slate-200/50 border border-slate-100 space-y-8">
            
            {/* Step 1: Doctor Selection */}
            {step === 1 && (
              <div className="space-y-6 animate-in fade-in slide-in-from-right-4 duration-500">
                <div className="flex items-center gap-3 pb-4 border-b border-slate-100">
                  <div className="p-3 bg-indigo-50 rounded-xl">
                    <Stethoscope size={24} className="text-indigo-600" />
                  </div>
                  <div>
                    <h2 className="text-xl font-bold text-slate-800">Choose a Specialist</h2>
                    <p className="text-sm text-slate-500">Select from our board-certified doctors</p>
                  </div>
                </div>

                <div className="space-y-4">
                  {doctors.map((doc) => (
                    <label
                      key={doc.id}
                      className={`flex items-start gap-6 p-5 rounded-2xl border-2 transition-all cursor-pointer group
                        ${form.doctor === doc.name 
                          ? 'border-indigo-500 bg-indigo-50/30 shadow-lg shadow-indigo-100' 
                          : 'border-slate-100 hover:border-indigo-200 hover:bg-slate-50'
                        }`}
                    >
                      <input
                        type="radio"
                        name="doctor"
                        value={doc.name}
                        checked={form.doctor === doc.name}
                        onChange={handleChange}
                        className="w-5 h-5 mt-1 text-indigo-600 focus:ring-indigo-500"
                      />
                      <div className="flex-1">
                        <div className="flex items-start justify-between">
                          <div>
                            <div className="flex items-center gap-3">
                              <div className={`w-14 h-14 rounded-xl flex items-center justify-center text-xl font-bold transition-all
                                ${form.doctor === doc.name 
                                  ? 'bg-indigo-600 text-white' 
                                  : 'bg-indigo-50 text-indigo-600 group-hover:bg-indigo-100'
                                }`}
                              >
                                {doc.image}
                              </div>
                              <div>
                                <p className="text-lg font-bold text-slate-800">{doc.name}</p>
                                <p className="text-sm font-semibold text-indigo-600">{doc.specialty}</p>
                              </div>
                            </div>
                          </div>
                          <div className="flex items-center gap-1 bg-amber-50 px-3 py-1.5 rounded-lg">
                            <Star size={14} className="text-amber-500 fill-amber-500" />
                            <span className="text-xs font-bold text-amber-700">{doc.rating}</span>
                            <span className="text-xs text-slate-400">({doc.reviews})</span>
                          </div>
                        </div>
                        
                        <div className="flex items-center gap-6 mt-4 text-xs">
                          <span className="flex items-center gap-1.5 text-slate-600">
                            <MapPin size={14} className="text-slate-400" />
                            {doc.location}
                          </span>
                          <span className={`flex items-center gap-1.5 font-medium
                            ${doc.nextAvailable === 'Today' ? 'text-emerald-600' : 'text-amber-600'}`}
                          >
                            <Clock size={14} />
                            Next: {doc.nextAvailable}
                          </span>
                        </div>
                      </div>
                    </label>
                  ))}
                </div>

                <div className="flex justify-end pt-6">
                  <button
                    type="button"
                    onClick={nextStep}
                    className="px-8 py-4 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl font-bold shadow-lg shadow-indigo-200 transition-all active:scale-[0.98] flex items-center gap-2"
                  >
                    Continue to Schedule
                    <ChevronRight size={18} />
                  </button>
                </div>
              </div>
            )}

            {/* Step 2: Date & Time Selection */}
            {step === 2 && (
              <div className="space-y-6 animate-in fade-in slide-in-from-right-4 duration-500">
                <div className="flex items-center gap-3 pb-4 border-b border-slate-100">
                  <div className="p-3 bg-indigo-50 rounded-xl">
                    <Clock size={24} className="text-indigo-600" />
                  </div>
                  <div>
                    <h2 className="text-xl font-bold text-slate-800">Select Date & Time</h2>
                    <p className="text-sm text-slate-500">Choose your preferred appointment slot</p>
                  </div>
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                  {/* Date Selection */}
                  <div className="space-y-4">
                    <label className="text-xs font-bold text-slate-500 uppercase tracking-wider ml-1 flex items-center gap-2">
                      <Calendar size={16} className="text-indigo-500" />
                      Preferred Date
                    </label>
                    <div className="relative group">
                      <Calendar className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 group-focus-within:text-indigo-600 transition-colors" size={20} />
                      <input
                        type="date"
                        name="date"
                        value={form.date}
                        onChange={handleChange}
                        required
                        min={new Date().toISOString().split('T')[0]}
                        className="w-full pl-12 pr-4 py-4 bg-slate-50 border border-slate-200 rounded-2xl text-sm focus:outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-500 transition-all"
                      />
                    </div>
                    <p className="text-xs text-slate-500 ml-1">* Appointments available Monday - Friday</p>
                  </div>

                  {/* Time Selection */}
                  <div className="space-y-4">
                    <label className="text-xs font-bold text-slate-500 uppercase tracking-wider ml-1 flex items-center gap-2">
                      <Clock size={16} className="text-indigo-500" />
                      Preferred Time
                    </label>
                    <select
                      name="time"
                      value={form.time}
                      onChange={handleChange}
                      required
                      className="w-full px-4 py-4 bg-slate-50 border border-slate-200 rounded-2xl text-sm focus:outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-500 transition-all appearance-none cursor-pointer"
                    >
                      <option value="">Select a time slot</option>
                      {timeSlots.map((time) => (
                        <option key={time} value={time}>{time}</option>
                      ))}
                    </select>
                  </div>
                </div>

                {/* Selected Doctor Summary */}
                <div className="bg-indigo-50/50 p-6 rounded-2xl border border-indigo-100">
                  <div className="flex items-center gap-3">
                    <div className="p-2 bg-white rounded-lg">
                      <User size={18} className="text-indigo-600" />
                    </div>
                    <p className="text-sm font-medium text-slate-700">
                      Selected Doctor: <span className="font-bold text-indigo-700">{form.doctor || "Not selected"}</span>
                    </p>
                  </div>
                </div>

                <div className="flex justify-between pt-6">
                  <button
                    type="button"
                    onClick={prevStep}
                    className="px-6 py-4 bg-slate-50 hover:bg-slate-100 text-slate-700 rounded-xl font-bold transition-all flex items-center gap-2"
                  >
                    <ChevronLeft size={18} />
                    Back
                  </button>
                  <button
                    type="button"
                    onClick={nextStep}
                    className="px-8 py-4 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl font-bold shadow-lg shadow-indigo-200 transition-all active:scale-[0.98] flex items-center gap-2"
                  >
                    Continue to Review
                    <ChevronRight size={18} />
                  </button>
                </div>
              </div>
            )}

            {/* Step 3: Confirmation & Details */}
            {step === 3 && (
              <div className="space-y-6 animate-in fade-in slide-in-from-right-4 duration-500">
                <div className="flex items-center gap-3 pb-4 border-b border-slate-100">
                  <div className="p-3 bg-indigo-50 rounded-xl">
                    <FileText size={24} className="text-indigo-600" />
                  </div>
                  <div>
                    <h2 className="text-xl font-bold text-slate-800">Confirm Appointment</h2>
                    <p className="text-sm text-slate-500">Review your details and add any notes</p>
                  </div>
                </div>

                {/* Appointment Summary */}
                <div className="bg-gradient-to-br from-slate-50 to-white rounded-2xl p-6 border border-slate-200 space-y-4">
                  <h3 className="text-sm font-bold text-slate-700 flex items-center gap-2">
                    <CheckCircle size={16} className="text-emerald-500" />
                    Appointment Summary
                  </h3>
                  
                  <div className="grid grid-cols-2 gap-4">
                    <div className="bg-white p-4 rounded-xl">
                      <p className="text-xs text-slate-500 mb-1">Doctor</p>
                      <p className="text-sm font-bold text-slate-800">{form.doctor}</p>
                      <p className="text-xs text-indigo-600 mt-1">
                        {doctors.find(d => d.name === form.doctor)?.specialty}
                      </p>
                    </div>
                    <div className="bg-white p-4 rounded-xl">
                      <p className="text-xs text-slate-500 mb-1">Date</p>
                      <p className="text-sm font-bold text-slate-800">{form.date}</p>
                    </div>
                    <div className="bg-white p-4 rounded-xl">
                      <p className="text-xs text-slate-500 mb-1">Time</p>
                      <p className="text-sm font-bold text-slate-800">{form.time}</p>
                    </div>
                    <div className="bg-white p-4 rounded-xl">
                      <p className="text-xs text-slate-500 mb-1">Patient Type</p>
                      <select
                        name="isNewPatient"
                        value={form.isNewPatient}
                        onChange={handleChange}
                        className="text-sm font-bold text-slate-800 bg-transparent border-none p-0 focus:ring-0"
                      >
                        <option value="yes">New Patient</option>
                        <option value="no">Existing Patient</option>
                      </select>
                    </div>
                  </div>
                </div>

                {/* Reason for Visit */}
                <div className="space-y-3">
                  <label className="text-xs font-bold text-slate-500 uppercase tracking-wider ml-1 flex items-center gap-2">
                    <FileText size={14} className="text-indigo-500" />
                    Reason for Visit (Optional)
                  </label>
                  <textarea
                    name="reason"
                    value={form.reason}
                    onChange={handleChange}
                    rows="4"
                    placeholder="Please briefly describe your symptoms or reason for the appointment..."
                    className="w-full px-4 py-4 bg-slate-50 border border-slate-200 rounded-2xl text-sm focus:outline-none focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-500 transition-all resize-none"
                  />
                </div>

                <div className="flex justify-between pt-6">
                  <button
                    type="button"
                    onClick={prevStep}
                    className="px-6 py-4 bg-slate-50 hover:bg-slate-100 text-slate-700 rounded-xl font-bold transition-all flex items-center gap-2"
                  >
                    <ChevronLeft size={18} />
                    Back
                  </button>
                  <button
                    type="submit"
                    className="px-8 py-4 bg-emerald-600 hover:bg-emerald-700 text-white rounded-xl font-bold shadow-lg shadow-emerald-200 transition-all active:scale-[0.98] flex items-center gap-2"
                  >
                    <Send size={18} />
                    Confirm Booking Request
                  </button>
                </div>
              </div>
            )}
          </div>
        </form>

        {/* Right: Informational Sidebar */}
        <div className="space-y-6">
          {/* Booking Policy Card */}
          <div className="bg-gradient-to-br from-slate-900 to-slate-800 rounded-[2rem] p-6 text-white shadow-2xl">
            <div className="flex items-center gap-3 mb-6">
              <div className="p-2 bg-indigo-500/20 rounded-lg">
                <Award size={24} className="text-indigo-300" />
              </div>
              <h3 className="font-bold text-lg">Booking Policy</h3>
            </div>
            
            <ul className="space-y-4">
              <li className="flex gap-3 text-sm">
                <span className="w-6 h-6 rounded-full bg-indigo-500/20 flex items-center justify-center flex-shrink-0 text-indigo-300 text-xs font-bold">1</span>
                <span className="text-slate-300">Requests are confirmed within <span className="text-white font-semibold">2 hours</span></span>
              </li>
              <li className="flex gap-3 text-sm">
                <span className="w-6 h-6 rounded-full bg-indigo-500/20 flex items-center justify-center flex-shrink-0 text-indigo-300 text-xs font-bold">2</span>
                <span className="text-slate-300">Cancel at least <span className="text-white font-semibold">24 hours</span> in advance</span>
              </li>
              <li className="flex gap-3 text-sm">
                <span className="w-6 h-6 rounded-full bg-indigo-500/20 flex items-center justify-center flex-shrink-0 text-indigo-300 text-xs font-bold">3</span>
                <span className="text-slate-300">Arrive <span className="text-white font-semibold">15 minutes early</span> for new patient forms</span>
              </li>
            </ul>

            <div className="mt-6 pt-6 border-t border-white/10">
              <div className="flex items-center justify-between">
                <span className="text-sm text-slate-400">Insurance accepted</span>
                <span className="text-xs font-bold text-emerald-400">✓ Most major providers</span>
              </div>
            </div>
          </div>

          {/* Selected Doctor Preview */}
          {form.doctor && (
            <div className="bg-white rounded-[2rem] p-6 shadow-xl border border-slate-100 animate-in slide-in-from-bottom-2 duration-500">
              <div className="flex items-center gap-4">
                <div className="w-14 h-14 rounded-xl bg-gradient-to-br from-indigo-500 to-indigo-600 flex items-center justify-center text-white text-xl font-bold shadow-lg shadow-indigo-200">
                  {doctors.find(d => d.name === form.doctor)?.image || 'DR'}
                </div>
                <div>
                  <h4 className="text-sm font-bold text-slate-500">Selected Doctor</h4>
                  <p className="text-lg font-bold text-slate-800">{form.doctor}</p>
                  <p className="text-xs text-indigo-600 font-semibold mt-1">
                    {doctors.find(d => d.name === form.doctor)?.specialty}
                  </p>
                </div>
              </div>
            </div>
          )}

          {/* Emergency Card */}
          <div className="bg-gradient-to-br from-amber-50 to-orange-50 rounded-[2rem] p-6 border border-amber-200 shadow-lg">
            <div className="flex items-center gap-4">
              <div className="p-3 bg-amber-100 rounded-xl">
                <AlertCircle className="text-amber-600" size={24} />
              </div>
              <div>
                <h4 className="text-sm font-bold text-amber-800">Emergency?</h4>
                <p className="text-xs text-amber-700 mt-1 font-medium">
                  If this is a life-threatening emergency, please call emergency services immediately.
                </p>
                <button className="mt-4 px-4 py-2 bg-amber-600 hover:bg-amber-700 text-white text-xs font-bold rounded-xl transition-all shadow-lg shadow-amber-200">
                  Call Emergency (911)
                </button>
              </div>
            </div>
          </div>

          {/* Contact Support */}
          <div className="bg-slate-50 rounded-2xl p-6 border border-slate-200">
            <div className="flex items-center gap-3">
              <div className="p-2 bg-slate-200 rounded-lg">
                <Phone size={18} className="text-slate-600" />
              </div>
              <div>
                <p className="text-xs text-slate-500">Need help booking?</p>
                <p className="text-sm font-bold text-slate-800">(555) 123-4567</p>
                <p className="text-xs text-slate-500 mt-1">support@healthcore.com</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default BookAppointment;



// pages/PatientDashboard.jsx
// Add MapPin to the end of the list
import { Calendar, Users, Pill, ArrowRight, Clock, Plus, Activity, Heart, Bell, TrendingUp, CheckCircle, AlertTriangle, MapPin } from "lucide-react";
import { Link } from "react-router-dom";
import { useState } from "react";

const PatientDashboard = () => {
  const [greeting] = useState(() => {
    const hour = new Date().getHours();
    if (hour < 12) return "Good morning";
    if (hour < 18) return "Good afternoon";
    return "Good evening";
  });

  const appointments = [
    { id: 1, doctor: "Dr. John Smith", specialty: "Cardiology", date: "Feb 12", time: "10:30 AM", status: "confirmed", location: "Main Medical Center - Room 304" },
    { id: 2, doctor: "Dr. Sarah Jenkins", specialty: "Neurology", date: "Feb 15", time: "2:15 PM", status: "pending", location: "West Wing - Floor 2" },
  ];

  const prescriptions = [
    { id: 1, name: "Amoxicillin", dose: "500mg", frequency: "Twice daily", status: "active", refill: "5 days left" },
    { id: 2, name: "Lisinopril", dose: "10mg", frequency: "Once daily", status: "active", refill: "12 days left" },
    { id: 3, name: "Metformin", dose: "850mg", frequency: "Twice daily", status: "expired", refill: "Expired" },
  ];

  const healthMetrics = [
    { label: "Heart Rate", value: "72", unit: "bpm", trend: "stable", color: "rose" },
    { label: "Blood Pressure", value: "118/76", unit: "mmHg", trend: "improving", color: "indigo" },
    { label: "Sleep", value: "7.5", unit: "hrs", trend: "good", color: "violet" },
    { label: "Steps", value: "8,234", unit: "steps", trend: "above average", color: "emerald" },
  ];

  const getStatusColor = (status) => {
    switch(status) {
      case 'confirmed': return 'bg-emerald-50 text-emerald-700 border-emerald-200';
      case 'pending': return 'bg-amber-50 text-amber-700 border-amber-200';
      case 'expired': return 'bg-slate-100 text-slate-600 border-slate-200';
      default: return 'bg-slate-50 text-slate-700 border-slate-200';
    }
  };

  const getTrendIcon = (trend) => {
    switch(trend) {
      case 'improving': return <TrendingUp size={14} className="text-emerald-500" />;
      case 'stable': return <CheckCircle size={14} className="text-blue-500" />;
      case 'good': return <CheckCircle size={14} className="text-emerald-500" />;
      default: return <Activity size={14} className="text-slate-400" />;
    }
  };

  return (
    <div className="max-w-7xl mx-auto px-4 py-8 space-y-8 animate-in fade-in duration-700">
      {/* Enhanced Welcome Header */}
      <div className="relative overflow-hidden bg-gradient-to-br from-indigo-600 via-indigo-500 to-purple-600 rounded-[2.5rem] p-8 text-white shadow-2xl">
        <div className="absolute top-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl transform translate-x-1/3 -translate-y-1/3"></div>
        <div className="absolute bottom-0 left-0 w-64 h-64 bg-purple-500/20 rounded-full blur-3xl transform -translate-x-1/3 translate-y-1/3"></div>
        
        <div className="relative z-10 flex flex-col md:flex-row md:items-center justify-between gap-6">
          <div className="space-y-4">
            <div className="flex items-center gap-3">
              <span className="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-semibold">
                {greeting}, Alex!
              </span>
              <span className="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-semibold flex items-center gap-2">
                <Bell size={14} />
                Member since 2023
              </span>
            </div>
            <div>
              <h1 className="text-4xl md:text-5xl font-black tracking-tight">Your Health Journey</h1>
              <p className="text-indigo-100 text-lg mt-3 max-w-2xl">
                Here's your personalized health overview. Your last checkup was 12 days ago.
              </p>
            </div>
          </div>
          
          <Link 
            to="/patient/book-appointment" 
            className="group flex items-center justify-center gap-3 bg-white text-indigo-600 px-8 py-4 rounded-2xl font-bold hover:shadow-2xl hover:scale-105 transition-all duration-300"
          >
            <Plus size={20} className="group-hover:rotate-90 transition-transform duration-300" />
            Book New Appointment
          </Link>
        </div>
      </div>

      {/* Health Metrics Grid */}
      <div className="grid grid-cols-2 lg:grid-cols-4 gap-6">
        {healthMetrics.map((metric, index) => (
          <div key={index} className="group relative bg-white rounded-2xl p-6 shadow-lg shadow-slate-200/50 border border-slate-100 hover:border-indigo-200 hover:shadow-xl transition-all duration-300">
            <div className={`absolute top-0 right-0 w-20 h-20 bg-${metric.color}-50 rounded-full blur-2xl opacity-50 group-hover:opacity-70 transition-opacity`}></div>
            <div className="relative">
              <div className={`inline-flex p-3 bg-${metric.color}-50 rounded-xl text-${metric.color}-600 mb-4 group-hover:scale-110 transition-transform duration-300`}>
                {metric.label === "Heart Rate" && <Heart size={20} />}
                {metric.label === "Blood Pressure" && <Activity size={20} />}
                {metric.label === "Sleep" && <Clock size={20} />}
                {metric.label === "Steps" && <TrendingUp size={20} />}
              </div>
              <div className="space-y-1">
                <p className="text-sm font-medium text-slate-500">{metric.label}</p>
                <div className="flex items-end gap-1">
                  <span className="text-2xl font-black text-slate-800">{metric.value}</span>
                  <span className="text-xs font-medium text-slate-400 mb-1">{metric.unit}</span>
                </div>
                <div className="flex items-center gap-1 mt-2">
                  {getTrendIcon(metric.trend)}
                  <span className="text-xs font-semibold text-slate-600 capitalize">{metric.trend}</span>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Main Dashboard Grid */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        {/* Upcoming Appointments - Enhanced */}
        <div className="lg:col-span-2 space-y-6">
          <div className="flex items-center justify-between">
            <h2 className="text-2xl font-bold text-slate-800 flex items-center gap-3">
              <span className="bg-indigo-50 p-2 rounded-xl">
                <Calendar size={20} className="text-indigo-600" />
              </span>
              Upcoming Appointments
            </h2>
            <Link to="/patient/my-appointments" className="text-sm font-semibold text-indigo-600 hover:text-indigo-700 hover:underline flex items-center gap-1">
              View all
              <ArrowRight size={16} />
            </Link>
          </div>

          <div className="space-y-4">
            {appointments.map((app, index) => (
              <div 
                key={app.id} 
                className="group bg-white rounded-2xl p-6 shadow-lg shadow-slate-200/50 border border-slate-100 hover:border-indigo-200 hover:shadow-xl transition-all duration-300 animate-in slide-in-from-bottom-2"
                style={{ animationDelay: `${index * 100}ms` }}
              >
                <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">
                  <div className="flex items-start gap-4">
                    <div className="relative">
                      <div className="w-14 h-14 rounded-2xl bg-gradient-to-br from-indigo-50 to-indigo-100 flex items-center justify-center">
                        <span className="text-xl font-black text-indigo-600">
                          {app.doctor.split(' ')[1]?.charAt(0) || 'D'}
                        </span>
                      </div>
                      <div className={`absolute -top-1 -right-1 w-3 h-3 rounded-full ${app.status === 'confirmed' ? 'bg-emerald-500' : 'bg-amber-500'} border-2 border-white`}></div>
                    </div>
                    <div>
                      <h3 className="text-lg font-bold text-slate-800">{app.doctor}</h3>
                      <p className="text-sm font-medium text-indigo-600">{app.specialty}</p>
                      <p className="text-xs text-slate-500 mt-1 flex items-center gap-1">
                        <MapPin size={12} />
                        {app.location}
                      </p>
                    </div>
                  </div>
                  
                  <div className="flex items-center gap-6 ml-16 md:ml-0">
                    <div className="text-right">
                      <p className="text-lg font-bold text-slate-800">{app.date}</p>
                      <p className="text-sm font-medium text-slate-500">{app.time}</p>
                    </div>
                    <span className={`px-3 py-1.5 rounded-lg text-xs font-bold border ${getStatusColor(app.status)}`}>
                      {app.status.charAt(0).toUpperCase() + app.status.slice(1)}
                    </span>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Right Column - Prescriptions & Quick Actions */}
        <div className="space-y-6">
          {/* Active Prescriptions */}
          <div className="bg-white rounded-2xl p-6 shadow-lg shadow-slate-200/50 border border-slate-100">
            <div className="flex items-center justify-between mb-6">
              <h3 className="text-lg font-bold text-slate-800 flex items-center gap-2">
                <span className="bg-emerald-50 p-2 rounded-lg">
                  <Pill size={18} className="text-emerald-600" />
                </span>
                Active Prescriptions
              </h3>
              <span className="text-xs font-semibold px-2 py-1 bg-emerald-50 text-emerald-700 rounded-lg">
                {prescriptions.filter(p => p.status === 'active').length} active
              </span>
            </div>
            
            <div className="space-y-4">
              {prescriptions.filter(p => p.status === 'active').map((pill, index) => (
                <div key={pill.id} className="flex items-center justify-between p-4 rounded-xl bg-slate-50 border border-slate-100 hover:border-emerald-200 hover:bg-emerald-50/30 transition-all duration-300 group">
                  <div className="flex items-center gap-3">
                    <div className="w-10 h-10 rounded-lg bg-white flex items-center justify-center text-emerald-600 group-hover:scale-110 transition-transform">
                      <Pill size={20} />
                    </div>
                    <div>
                      <p className="text-sm font-bold text-slate-800">{pill.name}</p>
                      <p className="text-xs text-slate-500">{pill.dose} • {pill.frequency}</p>
                    </div>
                  </div>
                  <div className="text-right">
                    <span className="text-xs font-semibold text-emerald-600 bg-emerald-100 px-2 py-1 rounded-lg">
                      {pill.refill}
                    </span>
                  </div>
                </div>
              ))}
            </div>
            
            <Link to="/patient/prescriptions" className="mt-6 block text-center text-sm font-semibold text-slate-600 hover:text-indigo-600 hover:bg-slate-50 py-3 rounded-xl transition-colors">
              View all medications
            </Link>
          </div>

          {/* Quick Actions */}
          <div className="bg-gradient-to-br from-slate-900 to-slate-800 rounded-2xl p-6 text-white shadow-xl">
            <h3 className="text-lg font-bold mb-4 flex items-center gap-2">
              <span className="bg-white/10 p-2 rounded-lg">
                <Clock size={18} />
              </span>
              Quick Actions
            </h3>
            <div className="space-y-3">
              <Link to="/patient/book-appointment" className="flex items-center justify-between p-4 bg-white/5 hover:bg-white/10 rounded-xl transition-all group">
                <span className="text-sm font-semibold">Schedule follow-up</span>
                <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" />
              </Link>
              <Link to="/patient/medical-records" className="flex items-center justify-between p-4 bg-white/5 hover:bg-white/10 rounded-xl transition-all group">
                <span className="text-sm font-semibold">View lab results</span>
                <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" />
              </Link>
              <Link to="/patient/messages" className="flex items-center justify-between p-4 bg-white/5 hover:bg-white/10 rounded-xl transition-all group">
                <span className="text-sm font-semibold">Message doctor</span>
                <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" />
              </Link>
              <Link to="/patient/billing" className="flex items-center justify-between p-4 bg-white/5 hover:bg-white/10 rounded-xl transition-all group">
                <span className="text-sm font-semibold">Pay bills</span>
                <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" />
              </Link>
            </div>
          </div>
        </div>
      </div>

      {/* Health Insights - Enhanced */}
      <div className="relative overflow-hidden bg-gradient-to-br from-indigo-900 via-indigo-800 to-purple-900 rounded-[2.5rem] p-8 text-white shadow-2xl">
        <div className="absolute top-0 right-0 w-96 h-96 bg-purple-500/20 rounded-full blur-3xl"></div>
        <div className="absolute bottom-0 left-0 w-64 h-64 bg-indigo-500/20 rounded-full blur-3xl"></div>
        
        <div className="relative z-10 flex flex-col lg:flex-row lg:items-center justify-between gap-8">
          <div className="space-y-4 max-w-2xl">
            <div className="flex items-center gap-3">
              <div className="p-3 bg-white/10 backdrop-blur-sm rounded-xl">
                <Heart size={24} className="text-rose-400" />
              </div>
              <h4 className="text-2xl font-bold">Health Insights</h4>
            </div>
            <p className="text-indigo-200 text-lg leading-relaxed">
              Based on your last lab results from November, your cholesterol levels have improved by <span className="text-emerald-400 font-bold">12%</span>. 
              Your blood pressure is now within the normal range. Keep up the excellent work!
            </p>
            <div className="flex items-center gap-6 pt-4">
              <button className="px-6 py-3 bg-white/10 backdrop-blur-sm hover:bg-white/20 rounded-xl font-semibold text-sm transition-all flex items-center gap-2">
                View Full Lab Report
                <ArrowRight size={16} />
              </button>
              <button className="px-6 py-3 text-indigo-200 hover:text-white font-semibold text-sm transition-colors">
                Dismiss
              </button>
            </div>
          </div>
          
          <div className="flex flex-col items-end">
            <div className="p-6 bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10">
              <div className="grid grid-cols-2 gap-6">
                <div className="text-center">
                  <p className="text-xs font-semibold text-indigo-300 uppercase tracking-wider mb-2">Next Checkup</p>
                  <p className="text-2xl font-black">Feb 28</p>
                  <p className="text-xs text-indigo-300 mt-1">in 12 days</p>
                </div>
                <div className="text-center border-l border-white/20 pl-6">
                  <p className="text-xs font-semibold text-indigo-300 uppercase tracking-wider mb-2">Health Score</p>
                  <p className="text-2xl font-black">85<span className="text-sm font-normal text-indigo-300">/100</span></p>
                  <p className="text-xs text-emerald-400 mt-1">↑ 5 points</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default PatientDashboard;


